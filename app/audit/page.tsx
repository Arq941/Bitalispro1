'use client';

import {useEffect,useState} from 'react';
import {Clock3,Loader2,ShieldCheck} from 'lucide-react';
import AppShell from '@/components/phase15/AppShell';
import {apiClient} from '@/lib/phase15/apiClient';

export default function AuditPage(){const[logs,setLogs]=useState<any[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{(async()=>{try{const j:any=await apiClient('/api/audit');setLogs(j?.data||[]);}catch(e:any){setError(e.message);}finally{setLoading(false);}})();},[]);return <AppShell title="Auditoría 360"><div className="mx-auto max-w-6xl px-4 py-5"><section className="rounded-[28px] bg-[#12224A] p-5 text-white"><div className="flex items-center gap-2 text-xs font-black uppercase tracking-[.14em] text-[#C79A3B]"><ShieldCheck className="h-4 w-4"/>Solo lectura</div><h1 className="mt-3 text-2xl font-black">Auditoría 360</h1><p className="mt-2 text-sm text-slate-300">Historial inmutable de acciones operativas y financieras.</p></section>{error&&<div className="mt-4 rounded-2xl border border-red-200 bg-red-50 p-4 text-sm font-bold text-red-700">{error}</div>}{loading?<div className="flex justify-center p-14"><Loader2 className="h-7 w-7 animate-spin text-[#12224A]"/></div>:<div className="mt-5 space-y-3">{logs.map((x:any)=><article key={x.id} className="rounded-3xl border border-slate-200 bg-white p-4 shadow-sm"><div className="flex items-start gap-3"><div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-2xl bg-slate-100"><Clock3 className="h-4 w-4 text-[#12224A]"/></div><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><span className="rounded-full bg-[#12224A] px-2.5 py-1 text-[9px] font-black text-white">{x.action}</span><span className="text-[10px] font-bold text-slate-400">{x.entity} · {x.entityId}</span></div><p className="mt-2 text-sm font-black text-[#12224A]">{x.user?[x.user.firstName,x.user.lastName].filter(Boolean).join(' ')||x.user.email:'Sistema'}</p><p className="mt-1 text-xs text-slate-500">{new Date(x.createdAt).toLocaleString('es-MX')} · {x.user?.role||'SYSTEM'} {x.ipAddress?`· IP ${x.ipAddress}`:''}</p></div></div></article>)}{!logs.length&&<div className="rounded-3xl border border-slate-200 bg-white p-8 text-center text-sm text-slate-500">Sin eventos de auditoría.</div>}</div>}</div></AppShell>}
