'use client';
import {FormEvent,useState} from 'react';
import {useSearchParams} from 'next/navigation';
import {CheckCircle2,KeyRound,Loader2} from 'lucide-react';

export default function SetPasswordPage(){const params=useSearchParams(),token=params.get('token')||'';const[password,setPassword]=useState(''),[confirm,setConfirm]=useState(''),[saving,setSaving]=useState(false),[error,setError]=useState(''),[done,setDone]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setError('');if(password!==confirm)return setError('Las contraseñas no coinciden.');setSaving(true);try{const res=await fetch('/api/auth/set-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,password})});const json=await res.json();if(!res.ok)throw new Error(json.error);setDone(true)}catch(e:any){setError(e.message||'No fue posible guardar la contraseña.')}finally{setSaving(false)}};return <main className="flex min-h-[100svh] items-center justify-center bg-[#F3F7F5] p-4"><section className="w-full max-w-md rounded-[28px] bg-white p-5 shadow-xl"><div className="mx-auto flex h-16 w-16 items-center justify-center rounded-3xl bg-[#062B24] text-white"><KeyRound/></div><h1 className="mt-4 text-center text-2xl font-black text-[#062B24]">Crear contraseña</h1><p className="mt-2 text-center text-sm text-slate-500">El enlace vence en 30 minutos y solo funciona una vez.</p>{error&&<div className="mt-4 rounded-2xl bg-red-50 p-3 text-sm font-bold text-red-700">{error}</div>}{done?<div className="mt-5 text-center"><CheckCircle2 className="mx-auto h-10 w-10 text-[#11A65A]"/><p className="mt-3 font-black">Contraseña guardada</p><a href="/" className="mt-4 flex min-h-12 items-center justify-center rounded-2xl bg-[#11A65A] font-black text-white">INICIAR SESIÓN</a></div>:<form onSubmit={submit} className="mt-5 grid gap-3"><input type="password" autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Nueva contraseña" className="min-h-14 rounded-2xl border px-4"/><input type="password" autoComplete="new-password" value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="Confirmar contraseña" className="min-h-14 rounded-2xl border px-4"/><p className="text-xs text-slate-500">Mínimo 8 caracteres, mayúscula, minúscula y número.</p><button disabled={saving||!token} className="flex min-h-14 items-center justify-center gap-2 rounded-2xl bg-[#11A65A] font-black text-white disabled:opacity-40">{saving?<Loader2 className="animate-spin"/>:<KeyRound/>}GUARDAR CONTRASEÑA</button></form>}</section></main>}
