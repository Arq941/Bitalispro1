'use client';

import {useEffect,useState} from 'react';
import {CheckCircle2,Loader2,ShieldCheck,XCircle} from 'lucide-react';
import AppShell from '@/components/phase15/AppShell';
import {apiClient} from '@/lib/phase15/apiClient';

type Item={id:string;type:string;status:string;reason?:string|null;createdAt:string;proposedPrice?:number|null;minimumPrice?:number|null;sale?:{saleNumber:string;client?:{firstName:string;lastName:string;clientNumber:string}};product?:{name:string;sku:string}};
export default function AuthorizationsPage(){const[items,setItems]=useState<Item[]>([]),[loading,setLoading]=useState(true),[busy,setBusy]=useState(''),[error,setError]=useState('');const load=async()=>{setLoading(true);try{const j:any=await apiClient('/api/authorizations');setItems(j?.data||[]);}catch(e:any){setError(e.message);}finally{setLoading(false);}};useEffect(()=>{load();},[]);const decide=async(id:string,decision:'APPROVED'|'REJECTED')=>{if(!confirm(decision==='APPROVED'?'¿Aprobar esta solicitud?':'¿Rechazar esta solicitud?'))return;setBusy(id);try{await apiClient(`/api/authorizations/${id}`,{method:'PATCH',body:JSON.stringify({decision})});await load();}catch(e:any){setError(e.message);}finally{setBusy('');}};return <AppShell title="Autorizaciones"><div className="mx-auto max-w-6xl px-4 py-5"><section className="rounded-[28px] bg-[#12224A] p-5 text-white"><div className="flex items-center gap-2 text-xs font-black uppercase tracking-[.14em] text-[#C79A3B]"><ShieldCheck className="h-4 w-4"/>Supervisión</div><h1 className="mt-3 text-2xl font-black">Pendientes de autorización</h1><p className="mt-2 text-sm text-slate-300">Precio, venta de dos productos y excepciones de crédito o enganche.</p></section>{error&&<div className="mt-4 rounded-2xl border border-red-200 bg-red-50 p-4 text-sm font-bold text-red-700">{error}</div>}{loading?<div className="mt-5 flex justify-center p-10"><Loader2 className="h-6 w-6 animate-spin text-[#12224A]"/></div>:<div className="mt-5 space-y-3">{items.filter(x=>x.status==='PENDING').length===0?<div className="rounded-3xl border border-slate-200 bg-white p-8 text-center text-sm text-slate-500">No hay autorizaciones pendientes.</div>:items.filter(x=>x.status==='PENDING').map(x=><article key={x.id} className="rounded-3xl border border-slate-200 bg-white p-4 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-3"><div><span className="rounded-full bg-orange-50 px-3 py-1 text-[10px] font-black text-[#FF6A00]">{x.type}</span><h2 className="mt-3 text-lg font-black text-[#12224A]">{x.sale?.client?`${x.sale.client.firstName} ${x.sale.client.lastName}`:x.product?.name||'Solicitud'}</h2><p className="mt-1 text-xs text-slate-500">{x.sale?.saleNumber||x.product?.sku||''} · {new Date(x.createdAt).toLocaleString('es-MX')}</p>{x.reason&&<p className="mt-3 text-sm text-slate-600">{x.reason}</p>}{x.proposedPrice!=null&&<p className="mt-2 text-sm font-bold">Propuesto ${x.proposedPrice.toLocaleString('es-MX')} {x.minimumPrice!=null&&<span className="text-slate-400">· mínimo ${x.minimumPrice.toLocaleString('es-MX')}</span>}</p>}</div><div className="grid min-w-52 grid-cols-2 gap-2"><button disabled={busy===x.id} onClick={()=>decide(x.id,'REJECTED')} className="flex min-h-12 items-center justify-center gap-2 rounded-2xl border border-red-200 bg-red-50 px-4 text-xs font-black text-red-700"><XCircle className="h-4 w-4"/>Rechazar</button><button disabled={busy===x.id} onClick={()=>decide(x.id,'APPROVED')} className="flex min-h-12 items-center justify-center gap-2 rounded-2xl bg-emerald-600 px-4 text-xs font-black text-white">{busy===x.id?<Loader2 className="h-4 w-4 animate-spin"/>:<CheckCircle2 className="h-4 w-4"/>}Aprobar</button></div></div></article>)}</div>}</div></AppShell>}
